name: Build Kernel X test
on:
  workflow_dispatch:

jobs:
  build:
    name: Runs-arm
    runs-on: ubuntu-24.04-arm
    steps:
    - name: test
      run: |
        sudo apt-get update
        sudo apt-get install git dwarves neofetch libelf-dev ccache automake flex lzop bison gperf build-essential zip curl zlib1g-dev g++-multilib libxml2-utils bzip2 libbz2-dev libbz2-1.0 libghc-bzlib-dev squashfs-tools pngcrush schedtool dpkg-dev liblz4-tool make optipng maven libssl-dev pwgen libswitch-perl policycoreutils minicom libxml-sax-base-perl libxml-simple-perl bc libc6-dev-i386 lib32ncurses-dev libx11-dev lib32z-dev libgl1-mesa-dev xsltproc unzip device-tree-compiler python3
        sudo lscpu
        sudo lsmem
        neofetch
